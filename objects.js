var playlist = {artistName: 'songTitle'};

function updatePlaylist(){
  return playlist.assign({}, )
} 

unction updateObjectWithKeyAndValue(obj, key, value) {
 
  return Object.assign({}, obj, { [key]: value })
}